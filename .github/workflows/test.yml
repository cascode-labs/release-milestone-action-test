name: Test

# Controls when the workflow will run
on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  milestone:
    types: [closed]

jobs:
  test-artifact:
    runs-on: ubuntu-latest
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - name: Clone repo to check
        run: |
          git clone git@github.com:cascode-labs/release-milestone-action.git
      - uses: ./release-milestone-action/actions.yml
      - name: Check Artifacts
        run: |
          ls -al
          if [ ! -f Release_Note_Log.md ]; then
            echo "Release Log was not created."
          else
            echo "Action successfully created release log."
          fi
